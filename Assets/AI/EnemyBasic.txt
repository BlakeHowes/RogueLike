tree("Root")
	sequence
		FindEnemies()
		HideInFog()
		tree("State")
tree("State")
	fallback
		tree("Idle")
		tree("Combat")
tree("Idle")
	sequence
		not isInCombat
		WalkRandomly
tree("Combat")
	while 
		isInCombat
		repeat
			sequence
				Wait(0.1)
				FindEnemies()
				tree("Actions")	
				Wait(0.2)
				checkEndTurn()
tree("Actions")
	fallback
		Attack()
		MoveToTarget()
		AdvanceToTarget()