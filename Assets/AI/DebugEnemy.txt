tree("Root")
	sequence
		FindEnemies()
		HideInFog()
		tree("State")
tree("State")
	fallback
		tree("Idle")
		tree("Combat")
tree("Idle")
	sequence
		not isInCombat
		WalkRandomly
tree("Combat")
	while
		ActionPointsAboveZero
		repeat
			sequence
				wait(0.2)
				
	EndTurn()
tree("Actions")
	fallback
		Attack()
		MoveToTarget()
		//AdvanceToTarget()